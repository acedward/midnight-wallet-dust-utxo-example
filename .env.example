# Host port mappings (copy to .env and adjust if defaults clash with other stacks)
NODE_PORT=29944
INDEXER_PORT=28088
PROOF_PORT=26300
API_PORT=3300

# Tuning
#TX_CONCURRENCY=8       # parallel tx pipelines in the api
#TARGET_UTXOS=20        # NIGHT UTXOs (= dust streams) per wallet
#OUTPUTS_PER_TX=10      # funding outputs per genesis tx
